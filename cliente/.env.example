# En local puedes dejar esto vacío (axiosConfig.js usa localhost:4000 por defecto).
# En producción (Vercel/Netlify), define esta variable con la URL real de tu backend:
VITE_API_URL="http://localhost:4000/api"
